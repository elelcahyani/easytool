fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
Pillow>=11.1.0
PyPDF2==3.0.1
aiofiles==24.1.0
python-dotenv==1.0.1
pdf2image==1.17.0
pikepdf>=10.3.0
pdf2docx==0.5.8
python-docx==1.1.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
slowapi==0.1.9
python-magic-bin==0.4.14  # For Windows file type detection

# Optional: For background removal (requires ~1GB model download on first use)
# Use rembg[cpu] for CPU support or rembg[gpu] for NVIDIA/CUDA GPU support
# IMPORTANT: Requires Python 3.8-3.12 (onnxruntime does not support Python 3.13+ yet)
rembg[cpu]>=2.0.68

# Note: Audio/Video tools require ffmpeg to be installed separately
# Download from: https://ffmpeg.org/download.html
