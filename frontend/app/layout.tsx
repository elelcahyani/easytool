import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Easy Tool - Free Online PDF & Image Tools',
  description: 'Free online tools for PDF and image processing. Merge, compress, split PDFs and compress images without registration.',
  keywords: 'pdf tools, image tools, compress pdf, merge pdf, split pdf, image compressor',
  icons: {
    icon: '/logo.png',
    apple: '/logo.png',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
